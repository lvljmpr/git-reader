name: git-reader demo
on:
  push:
    branches: [none]
jobs:
  install-deps:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v3
      with:
        repository: ''
        ref: ''
        token: ''
    - run: |
        git-reader.sh
